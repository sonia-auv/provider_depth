<launch>

    <!-- load the provider -->
    <node name="provider_depth" pkg="provider_depth" type="provider_depth_node" output="screen"/>

    <rosparam command="load" file="$(find provider_depth)/config/$(env AUV).yaml"/>

</launch>